##################
Project required jdk 1.8 and maven 3.
It's built using spring boot and spring data
To run the application the class StateStreetService can be run
Alternatively to run from command line use    mvn spring-boot:run 

The various CRUD operations are covered under run method and leverages spring data's built in CRUD repository.
To store course scores a join table with extra column can be created.

